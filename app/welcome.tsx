// TODO: draw out welcome screen with all required data
// TODO: set up saving and generating default settings.json data

// todo: create welcome screen component
// todo: set up greeting from settings.json (hello, universe!)
// todo: set up loading each changable setting
// todo: set up loading exit button

// RecordKit: name of app
// "Find your normal, and keep it."
// Your data is stored only on this device unless you upload it to your own online storage platform.
// Data does not exit this app unless you instruct it to.
// The developer of this app believes in full ownership of your own data.

// settingsFolder must be set with a default by-operating-system
// UUID *must* be randomised on initialisation of app for each user - give warning to not share UUID
// dayFormat default can be grabbed from OS and if not, this default
// timeFormat same as day
// colourScheme only light and dark (both grayscale) for default, default matches OS setting by default

// Change the settings below according to your preferences.

// Change these settings at any time under Settings.

/*
* Open Journal: name of app
*   save settings.json
*/

// send to Home Screen
